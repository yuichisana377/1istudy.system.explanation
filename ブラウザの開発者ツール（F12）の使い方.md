これまでの記事では「コードを読んで理解する」話をしてきましたが、今回は視点を変えて、**動いている実際のページを、コードを一切いじらずに覗き見る・確かめる**ための道具、ブラウザの「開発者ツール（DevTools）」の使い方を書きます。

CardMakerやTimetableなど、このリポジトリで解説してきたページを実際に開きながら、ここに書いてあることを一つずつ試してみると、コードの説明が「本当にそうなっている」と自分の目で確認できて理解が定着しやすいはずです。開発者ツールはこのサイトに限らず、**世の中のどんなWebページに対しても使える**ので、覚えておいて損はありません。

---

## 1. 開き方

だいたいどのブラウザでも、次のどれかで開けます。

- キーボードで **F12** キーを押す（Windowsで一番手軽）
- ページの上で右クリックして「**検証**」（Chrome/Edge）または「**要素を調査**」（Firefox）を選ぶ
- キーボードショートカット **Ctrl+Shift+I**（Windows）／**Cmd+Option+I**（Mac）

スマホ実機には無いので、確認したいときはPCのブラウザで開くのが基本です（後述の「デバイスツールバー」でスマホ表示を再現することはできます）。

開くと画面の右側か下側に新しいパネルが出てきて、上部にいくつかタブが並びます（**Elements**／**Console**／**Network**／**Application**／**Sources** など）。これから、この中でよく使うものを順番に説明します。

---

## 2. Elements（要素）タブ — 今の画面のHTML/CSSをその場で覗く

**「今ブラウザに実際に表示されている」HTML（DOM）とCSSを、そのままの形で見られるタブ**です。

- 画面の左上にある矢印アイコン（要素の選択ツール）をクリックしてから、ページ上の気になる部分（ボタンやカードなど）をクリックすると、それに対応するHTMLの行がElementsパネル上でハイライトされてジャンプします。「この見た目、HTML上ではどう書かれているんだろう？」を知りたいときはこれが一番早い方法です。
- 右側の「Styles」欄には、その要素に効いているCSSのルールが、優先順位付きで並びます。取り消し線が引かれているルールは「書かれてはいるけど、もっと優先度の高い別のルールに上書きされて効いていない」という意味です。
- HTML・CSSともに、その場でダブルクリックして値を書き換えられます。**ただしこれは「今見ている自分の画面上だけ」の変更で、ページを再読み込みすれば元に戻ります**（サーバー上のファイルが書き換わるわけではありません）。「もし枠線を赤にしたら」「もしこのdivを消したら」を安全に試せる、という理解で使うと便利です。

例えば[CardMakerの解説](1I勉強会web解説/02_Cardmaker/00_HTML構造とページ全体像.md)で触れた「`<div class="screen">`を全部HTML内に用意しておいて、`showScreen()`が`active`クラスを付け替えるだけの簡易ルーター」という仕組みも、Cardmaker.htmlを開いてElementsタブで`class="screen"`を検索すれば、画面を切り替えるたびに`active`クラスがどのdivに付いたり消えたりするかを実際に目で追えます。

---

## 3. Console（コンソール）タブ — エラーの表示とJSの実行

**JavaScriptが出力したメッセージや、実行時に起きたエラーが表示される場所**です。このサイトのコードにも、あちこちに`console.warn(...)`が仕込まれています。例えば[Cardmaker.jsの解説](1I勉強会web解説/02_Cardmaker/03_Cardmaker.js_その3_デッキの読み込みと作成編集.md)に出てくる

```js
console.warn(`[cardmaker] get_card_set が${fetchedCards.length}件しか返しませんでしたが、${expectedCount}件のはずです。 filename=${deck.filename}`);
```

のような行は、普段は画面上には何も出ませんが、Consoleタブを開いていれば黄色い警告として表示されます。同じように[遅延読み込みチャンクの解説](1I勉強会web解説/02_Cardmaker/08_Cardmaker.js_その8_画像処理と基盤機能.md)にある`console.warn('[cardmaker]', e)`のように、`catch`ブロックの中で「エラーを画面には出さず、開発者だけが気づけるように記録しておく」という使い方もよくされています。

- **赤字はエラー**（JSの実行が途中で止まってしまうような問題）、**黄色は警告**（動作はするけど注意した方がいい）です。
- エラーの行をクリックすると、原因になったJSファイルの該当行にジャンプできます（Sourcesタブ、後述）。
- このサイトの過去の運用で実際にあった話として、Pythonの一括置換スクリプトのミスでJSの構文が壊れてしまい（例えば`<svg>...</svg>`という生のHTML文字列が、クォートで囲まれずJSのコードの中に直接紛れ込んでしまうミス）、ページの機能が丸ごと動かなくなる、という事故が何度かありました。こういう「見た目には何が起きているか分からない、白い画面のまま」のようなバグは、Consoleを開いて`SyntaxError`のような赤いエラーが出ていないかを確認するのが、原因を突き止める一番確実な方法です。
- Consoleには直接JavaScriptのコードを打ち込んで、その場で実行することもできます。例えば`document.querySelectorAll('.deck-card').length`と打てば、今の画面に表示されているデッキカードの数をその場で数えられます。ただし、**知らない人から「このコードをコンソールに貼り付けて」と言われても、絶対に貼り付けないでください**。コンソールから実行したJSは、今ログインしているアカウントの権限で好き放題に動けてしまうため、これを悪用してパスワードやセッション情報を盗む詐欺（self-XSS）が実際に存在します。

---

## 4. Network（ネットワーク）タブ — どんな通信をしているか

**ページがサーバーとどんな通信をしているか**を、1件ずつ全部見られるタブです。このシリーズで繰り返し出てきた「`fetch()`でAPIを呼ぶ」「SSEで更新通知を受け取る」といった話を、実際の通信として目で確認できます。

- タブを開いた状態でページを操作すると、通信が起きるたびに一覧に行が増えていきます。列には、呼び出したURL（例：`/api/list_cards`）・HTTPメソッド（`GET`/`POST`）・ステータスコード（`200`なら成功、`404`なら見つからない、`401`ならログインが必要、など）・かかった時間が並びます。
- 1件の通信をクリックすると、詳細が右側（または下側）に出ます。
  - **Headers**：送ったリクエストの中身が見られます。[StudyLog.jsのタイマー機能の解説](1I勉強会web解説/04_StudyLog/04_StudyLog.js_その4_タイマー機能.md)などで触れた「`session_token`はURLクエリに載せず、`Authorization: Bearer <token>`ヘッダーで送る」という設計も、ここでリクエストヘッダーを開けば実際にそうなっていることが確認できます。
  - **Payload / Request**：POSTで送ったJSONの中身（`{guild_id: ..., session_token: ...}`など）が見られます。
  - **Response**：サーバーが実際に返してきたJSONがそのまま見られます。「保存できたのはいいけど、サーバーは実際どんなデータを返してきているんだろう？」を知りたいときはここです。
  - **Timing**：通信にどれくらい時間がかかったかの内訳です。
- フィルタ欄に`XHR`や`Fetch`を選ぶと、画像やCSSなどのファイル読み込みを除いた、実際のAPI通信だけに絞り込めます。
- `events`という名前で、ずっと接続が切れずに残り続けている行が見つかるはずです。これが[各ページで使われているSSE（Server-Sent Events）](1I勉強会web解説/01_index_予定管理.md)の接続で、クリックすると「EventStream」というタブから、サーバー側で何かデータが変わるたびに送られてくる通知を、リアルタイムで観察できます。
- ネットワークを意図的に遅くしたり、切断したりするシミュレーション機能もあります（上部の「No throttling」というプルダウン、または「Offline」のチェック）。「通信が遅い環境で、このボタンの連打防止はちゃんと効くか」「オフラインになったときエラー表示はちゃんと出るか」を確認したいときに使えます。

**ここで学べる大事なこと**：Networkタブを見れば、フォームに何を入力してボタンを押すと、実際にはどんなHTTPリクエストが飛んでいるかが丸見えになります。裏を返すと、**利用者は画面の入力欄を経由しなくても、同じAPIに直接好きな値を投げつけることができてしまう**ということです。このシリーズで何度も出てきた「クライアント側のチェックはあくまで親切のためで、最終的な防衛は必ずサーバー側にもある」（[StudyLog.jsの手入力の解説](1I勉強会web解説/04_StudyLog/03_StudyLog.js_その3_タブ表示・手入力・課題達成.md)など）という話は、まさにこのNetworkタブで簡単に確認できる事実が前提になっています。

---

## 5. Application（アプリケーション）タブ — 端末に保存されているデータ

Chrome/Edgeでは「**Application**」、Firefoxでは「**ストレージ**」という名前のタブです。**この端末（ブラウザ）の中に保存されているデータ**を確認できます。

- 左側のツリーから「**Local Storage**」を開き、このサイトのドメインを選ぶと、`localStorage`に保存されているキーと値の一覧が見られます。このシリーズで何度も登場した保存先が、実際にここに並んでいます。
  - `sl_session`：ログインセッション（[Login.jsの解説](1I勉強会web解説/09_Login/00_Login解説.md)）
  - `cardmaker_study_data_cache_v1`：CardMakerの「わからない」マークや学習の続き（[Cardmaker.jsその6の解説](1I勉強会web解説/02_Cardmaker/06_Cardmaker.js_その6_カード編集と学習データ同期.md)）
  - `tancheck_scores_v2`：単位チェッカーの入力値（[TanCheck.jsの解説](1I勉強会web解説/07_TanCheck/00_TanCheck解説.md)、サーバーには一切送られずここだけに残る、という話の実物です）
  - `tt_overrides_<guild_id>`：時間割の変更データのローカルキャッシュ
- 値をダブルクリックすれば、その場で書き換えたり消したりもできます。「ログイン状態を試しに消してみて、未ログイン時の挙動を確認する」「下書きが保存されているはずのキーを見て、本当にJSONとして中身が入っているか確認する」といった動作確認に使えます。
- 同じ場所に「**Session Storage**」もあります。こちらは`localStorage`と違い、**タブを閉じると消える**一時的な保存先で、[Login.jsの解説](1I勉強会web解説/09_Login/00_Login解説.md)に出てくる「ログイン後に戻る先のURL」（`post_login_redirect`）などがここに入ります。
- 「**Cookies**」の欄も見られますが、このサイトはセッションを`localStorage`で管理しているため、認証まわりのCookieは基本的に使われていません（このあたりの設計判断は[Webの裏側の話](1I勉強会webの裏側（設計の話）.md)でも触れています）。

**注意点**：Local StorageやSession Storageの中身は、暗号化されていないただの文字列です。開発者ツールを使えば誰でも読めてしまうため、「他人に見られたら困る情報（パスワードそのものなど）」をここに保存するのは避けるべき、という設計上の教訓にもなります（このサイトが`session_token`という「本人確認用の合言葉」だけを保存し、パスワードそのものは扱わない設計にしているのも、こうした理由と無関係ではありません）。

---

## 6. Sources（ソース）タブ — 実際のJSファイルを読む・止めて調べる

**ブラウザが実際に読み込んでいるJS/CSS/HTMLファイルそのもの**を見られるタブです。このリポジトリの解説記事で読んでいるコードと、寸分違わず同じもの（本番用に圧縮されていなければ）がここに表示されます。

- 左側のファイルツリーから`Cardmaker.js`のようなファイルを選べば、中身がそのまま読めます。
- 行番号をクリックすると、そこに**ブレークポイント**（一時停止ポイント）を置けます。ページを操作して、その行にコードの実行が到達すると、そこで処理が一時停止し、**その瞬間の変数の中身**を右側のパネルで確認できます。「`if`の条件分岐で、実際にどっちに進んだのか」「この変数、この時点で本当に想定通りの値が入っているのか」を、`console.log`を書き足してページを直さなくても確認できる、強力な機能です。
- 一時停止した状態で、右側の「▶（Resume）」「⤵（Step over）」「⤵（Step into）」といったボタンで、1行ずつコードを進めながら動きを追うこともできます。
- Consoleタブの下部に小さな入力欄（「Watch」欄）を出せば、一時停止中に特定の変数の値だけをずっと見張っておくこともできます。

このサイトはビルドツールを使わずに素のJS/CSSをそのまま読み込ませる構成（[Webの裏側の話](1I勉強会webの裏側（設計の話）.md)参照）なので、圧縮・難読化されておらず、Sourcesタブでとても読みやすいのも特徴です（世の中の多くのWebサービスは、ここが圧縮された1行の読みにくいコードになっていることが多いです）。

---

## 7. デバイスツールバー — スマホ表示を再現する

Elementsタブの左上、あるいはメインツールバーにある「スマホとタブレットのアイコン」（Ctrl+Shift+M）を押すと、画面がスマホ・タブレットの画面サイズをシミュレートした表示に切り替わります。

- 上部のプルダウンから「iPhone 14」のような機種を選んだり、画面サイズを自由な数値で指定したりできます。
- タッチ操作のシミュレーション（マウスのクリックを「タップ」として扱う）もできるので、[CardMakerのホーム画面ドラッグ並び替えの解説](1I勉強会web解説/02_Cardmaker/05_Cardmaker.js_その5_ホーム画面のドラッグ並び替え.md)で説明したような、Pointer Events（`pointerdown`/`pointermove`/`pointerup`）を使った長押しドラッグの挙動を、PCの画面上でスマホっぽく試すことができます。
- ただし、あくまで「画面サイズと基本的な操作」を再現しているだけで、実機と完全に同じ挙動になるとは限りません（このリポジトリの過去の作業記録にも「実機での見た目確認はできていない」という注記が何度か出てきます）。最終確認は、できれば実際のスマホで行うのが確実です。

---

## まとめ：開発者ツールで「動いているコード」を裏取りする

このシリーズの解説記事は、コード（`bot.py`や`Cardmaker.js`など）を読んで「こう動くはずです」と説明する形式でしたが、開発者ツールを使えば、それが**実際にそうなっているかを自分の目で確かめる**ことができます。

- Elements：今の画面のHTML/CSSの実物を見る
- Console：エラー・警告を見る、JSをその場で試す
- Network：サーバーとの通信を1件ずつ見る（何を送って、何が返ってきているか）
- Application：`localStorage`など、端末に保存されているデータを見る
- Sources：実際のJSファイルを読み、一時停止させて変数の中身を確認する

特にNetworkタブは、「クライアント側の入力チェックは親切のためのものであって、本当の防御はサーバー側にある」という、このシリーズで繰り返し出てきた考え方を、実感として理解する一番の近道です。実際に手を動かして、気になるページのタブを覗いてみてください。
