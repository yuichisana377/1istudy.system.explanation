このサイト（1I勉強会web / Discord Bot）は「使いやすさ」だけでなく、仕組みやセキュリティを実地で学ぶ場としても作っています。せっかく情報工学科の仲間内で使ってもらっているので、今回はかなり細かいところまで書いてみます。長いですが、気になるところだけ拾い読みしてもらっても大丈夫です。

## 1. データの持ち方

### 1.1 なぜデータベースを使っていないか

MySQLやPostgreSQLのような本格的なRDBは使っていません。予定・時間割・カードデッキ・お知らせ・ユーザー情報まで、全部 **サーバーのディスク上のJSONファイル** です。11人規模の運用でRDBを持ち出すのは正直オーバースペックで、JSONなら「テキストエディタで開いて読める・壊れたときに人間が手で直せる・バックアップがただのファイルコピーで済む」というメリットの方が大きいと判断しています。

### 1.2 実際のファイル一覧

`DATA_DIR`というディレクトリの直下に、機能ごとにファイルが分かれています（`{guild_id}`の部分はDiscordサーバーのID、今は`1509880344806162544`固定）。

**生徒・ログイン関連**
- `users_{guild_id}.json` … 学籍番号・ニックネーム・作成日（旧パスワード方式の名残でハッシュ化されたパスワードのフィールドが残っている生徒データもあるが、今は一切参照されない死んだフィールド）
- `discord_login_links_{guild_id}.json` … ログイン専用の「学籍番号 ⇔ DiscordユーザーID」対応表
- `discord_links_{guild_id}.json` … DM通知専用の「学籍番号 ⇔ DiscordユーザーID」対応表（ログイン用と別ファイルにしてあるのは、「DM通知だけ受け取りたいが学籍番号ログインのままにしたい」といった過去の運用と互換性を保つため）

**予定・時間割**
- `plans_{guild_id}.json` … 予定（宿題・提出・テストなど）
- `timetable_{guild_id}.json` … 通常時間割からの上書き（休講・時限変更など）
- `terms_{guild_id}.json` … 学期ごとの基本時間割（曜日×時限の科目表）

**勉強ログ・ポイント・タイマー**
- `study_logs_{guild_id}.json` … 全員共有の勉強時間記録（「みんなの記録」の元データ）
- `study_timers_{guild_id}.json` … 今まさに計測中のタイマー状態（端末をまたいでも同期するための共有状態）
- `points_{guild_id}.json` … 生徒ごとの累計ポイント（本人にしか表示しない設計）
- `completed_tasks_{guild_id}.json` … 達成済み課題の記録

**CardMaker（暗記カード）**
- `words/*.json` … カードデッキ本体1件につき1ファイル（ファイル名はハッシュのような形式で中身の推測はできない）
- `cards_index.json` … 全デッキの一覧を高速に返すための索引（キャッシュ）。デッキ本体を全部読まなくても一覧表示できるようにするための工夫
- `folders.json` … デッキを整理するフォルダの階層構造
- `list_order.json` … デッキ・フォルダの並び順（ドラッグ操作の結果）
- `in_progress_decks.json` … 「今誰がどのデッキを編集中か」の目印（他人と同時編集して上書き事故が起きるのを防ぐための表示専用データ）
- `quiz_leaderboard_{デッキのファイル名}.json` … 「みんなでクイズ」のスコアランキング

**お知らせ**
- `notices/*.md` / `notices/*.txt` … お知らせ本文そのもの（Markdown対応）
- `notices_meta.json` … 各お知らせの投稿者・投稿日時・実行済みフラグなどのメタ情報

**運用まわり**
- `system_log.json` … サービス情報ページの「運用ログ」で表示している監査ログ（誰が・何を・どう変えたかの記録、GitHubのコミット風の差分付き）
- `logs_{guild_id}.json` … こちらはもっと古くから存在する内部用の簡易ログ（30日でロールオーバー）。system_log.jsonの方が新しく詳しい実装で、今はほぼこちらが本体
- `pending_delete_requests_{guild_id}.json` … 作成者本人以外がデッキ/お知らせを削除しようとした際、DiscordのDM連携が無い相手への「Web上での確認待ち」を控えておく場所
- `config_{guild_id}.json` … サーバー単位の各種設定

### 1.3 セッション（ログイン状態）の仕組み

サーバー側に「ログイン中のユーザー一覧」のようなテーブルは持っていません。ログイン成功時に発行するトークンは、`{学籍番号, サーバーID, 発行時刻}` というJSONをBase64化し、そのうしろに**HMAC-SHA256の署名**をくっつけただけの文字列です（`base64(payload).HMAC(secret, base64(payload))`）。JWT（JSON Web Token）の簡易自作版のようなものだと思ってもらえれば近いです。

これの良いところは、サーバーを再起動してもセッション情報がメモリから消えて全員ログアウト……ということが起きない点（署名の秘密鍵さえ変わらなければ検証できる）。逆に弱点は、**発行済みのトークン1本だけをピンポイントで無効化する仕組みが無い**ことです。「全員ログアウトさせたい」ときは、①秘密鍵ごと変える（本当に全員が対象）か、②「この時刻より前に発行されたトークンは無効」という足切り線を引くか、のどちらかしかできません。実際、8/20にログイン方式をパスワードからDiscordへ一本化したときは②のやり方で、旧パスワード方式のセッションを含めて一括で失効させました。

有効期限は発行から30日間。それを過ぎるか、対象サーバーのDiscordメンバーでなくなっている（＝「制限付きアカウント」）と判定されると、予定一覧の閲覧以外の機能が使えなくなります。

### 1.4 同時に書き込まれたときの対策

複数人が同時に同じファイルを書き換えようとすると、後から保存した方が先の変更を丸ごと消してしまう事故（lost update）が起きます。これを防ぐために、読み込み時にファイルのハッシュ値（SHA）を一緒に取得しておき、保存時に「今のファイルのハッシュ値が、自分が読み込んだときと同じか」をチェックする、**楽観的排他制御（optimistic concurrency control）** に近いやり方を使っています。ズレていたら「誰かが先に書き換えた」と判断して、最新の内容を読み直してから自分の変更を乗せ直し、保存をリトライします。

### 1.5 バックアップ

生徒の学習データはサーバーのローカルディスクにしか無いので、消えたら詰みます。そのため**毎晩0時（JST）に、プライベートなGitHubリポジトリへ自動でバックアップ**する仕組みを動かしています。対象は生徒個人が特定できるデータファイル（`*.json`・`notices/`・`words/`）だけに絞ってあり、Botのコード本体（`bot.py`）や`.env`のような秘密情報はバックアップの対象から意図的に外しています。

## 2. Discordまわりの設定（Developer Portal側）

このサイトはDiscordと2種類の関わり方をしています。

### 2.1 ログイン用のOAuth2連携

「Discordでログイン」ボタンを押すと、Discord Developer PortalでこのアプリのOAuth2設定に登録してある **Client ID**（`1515358957542047975`。これは秘密情報ではなく、ログイン画面のURLを見れば誰でも分かる公開情報）を使って認可画面に飛びます。要求しているスコープは **`identify`だけ**（ユーザーID・ユーザー名・アイコンなど最低限のプロフィール情報のみ。メールアドレスやサーバー参加状況そのものは`identify`スコープでは取得できません）。

認可が終わって戻ってくる先（Redirect URI）はDeveloper Portal側に事前登録しておく必要があり、1文字でも違うと弾かれます。この戻り先はサーバーの実ホスト名で直接受けているため、サイトの他の部分のように匿名化されたドメイン経由にはできていません。

サーバーへの参加確認（「制限付きアカウント」の判定）は、この`identify`スコープでは分からないので、OAuthのユーザー情報ではなく **Bot自身がすでに持っているサーバーメンバー一覧のキャッシュ** と突き合わせて判定しています。

### 2.2 Bot本体の権限（Privileged Gateway Intents）

Discordの「Bot」は、OAuthログインとは別枠で、常時サーバーに接続し続けてコマンドを処理する常駐プロセスです。Developer PortalのBot設定で、以下3つの **Privileged Gateway Intents**（デフォルトでは無効になっていて、明示的にONにしないとDiscord側にブロックされる特権インテント）を有効化しています。

- **SERVER MEMBERS INTENT** … サーバーの全メンバー一覧を取得・キャッシュする。制限付きアカウント判定（対象サーバーのメンバーかどうか）に必須
- **PRESENCE INTENT** … メンバーのオンライン状態を取得する
- **MESSAGE CONTENT INTENT** … メッセージの本文を読み取る

このうちMESSAGE CONTENT INTENTは、Botが一定人数以上のサーバーに導入される場合はDiscord社の審査（Bot Verification）が必要になる類の権限ですが、このBotは1サーバー限定の身内運用なので審査対象外です。

### 2.3 スラッシュコマンド

`/id連携`のようなDiscord上のスラッシュコマンドは、`bot.tree`というdiscord.pyの仕組み（Application Commands）で実装・登録しています。起動時に`bot.tree.sync()`を呼ぶと、登録済みのコマンド定義がDiscord側に反映される仕組みです。現状のコマンドは `/add`（予定追加）・`/list`（予定一覧）・`/delete`（予定削除）・`/edit`（予定編集）・`/setchannel`（通知チャンネル設定）・`/setup_roles`（通生/寮生の振り分けパネル投稿）・`/id連携`・`/help` の8種類です。

## 3. セキュリティまわり

**認証方式**：もともと「学籍番号＋パスワード」で始めましたが、2026/08/20にDiscordログインのみへ一本化しました。パスワードを運用するということは、それ自体が攻撃対象（総当たり・使い回し・漏洩）を増やす行為なので、学校のDiscordサーバーという既に信頼している基盤にログインを寄せることで、パスワード管理という問題そのものをなくしています。あわせて、対象サーバーに参加していないアカウントは編集操作や大半の機能を使えない「制限付きアカウント」として扱うようにし、新しいログインが検出されるたびに本人のDiscordへ通知が飛ぶようにしました（乗っ取られたときに気づけるように）。

**XSS対策**：ユーザーが入力した文字列（ニックネーム・お知らせ本文・カードの問題文など）をそのまま`innerHTML`に差し込むと、`<img src=x onerror=...>`のような文字列で任意のJSを実行されてしまいます。過去に実際、お知らせ機能やCardMakerの画像プレビューでこの対策が漏れていた箇所が見つかり、全て`esc()`によるエスケープか、`textContent`/`createElement`を使うDOM APIベースの組み立てに直しました。

**総当たり対策**：ログインやパスワード関連のAPI（今は主に確認コード系）には、同一IPからの試行回数を数える簡易的なレート制限をかけています（15分間に10回まで、超えると`429 Too Many Requests`で一時拒否）。

**CSP / HSTSなどのレスポンスヘッダー**：`Content-Security-Policy`・`X-Frame-Options: DENY`・`X-Content-Type-Options: nosniff`・`Strict-Transport-Security`（HTTPS強制、1年間・サブドメイン含む）・`Permissions-Policy`（位置情報・カメラ・マイクを無効化）などを設定し、クリックジャッキングやMIMEスニッフィング、プロトコルダウングレードへの対策をしています。

正直に書くと、**あえてやっていないこと**もあります。CSPは`script-src`に`unsafe-inline`を許可したままで、これは主要な脆弱性というより「多層防御（defense in depth）の一枚」を諦めている状態です。除去するにはサイト全体の`onclick="..."`を`addEventListener`に書き換えるか、リクエストごとにnonceを発行する仕組みが必要で、11人規模の運用に対して今すぐ投資するには見合わないと判断し保留にしています。セッションを`localStorage`ではなくhttpOnly Cookieに置く案も同様の理由で保留中です。「理想を全部やる」のではなく「今のリスクと運用規模に対して、どこまで投資するか」を都度判断しているという意味で、これも一つの学びだと思っています。

## 4. 読み込みを速くする工夫

**ポーリングをやめてSSE（Server-Sent Events）にした**：以前はお知らせ・勉強タイマーなどを「10秒ごとに問い合わせる」ポーリング方式で更新していましたが、これだと最大10秒のラグがある上に、変化が無くても延々とリクエストが飛び続けます。今は`/events`という1本の常時接続（`text/event-stream`）を張っておき、サーバー側でデータが変わった瞬間だけ`notify_change()`で全端末にプッシュ通知する方式に切り替えました。反映がほぼ瞬時になり、無駄なリクエストも激減しています。

**ページ遷移の先読み（prefetch）**：ドロワーメニューを開いた瞬間（＝まだどのページに行くか決める前）に、他ページのJS/CSSファイルをバックグラウンドで先読みしておく仕組みを入れています。実際にメニューをタップする頃にはブラウザキャッシュに乗っていることが多く、体感の切り替え速度が上がります。

**キャッシュ制御の使い分け**：`list_cards`や`timer_state`のようにデータが頻繁に変わるGET系APIには、サーバー側で明示的に`Cache-Control: no-store`を付けています。これは高速化と逆の指定に見えますが、実は「ブラウザ（特にChrome）が古いレスポンスを勝手にディスクキャッシュしてしまい、新しく作ったデッキがすぐ反映されない」という不具合の対策で、正しさを優先した設計判断です。一方で静的ファイル（JS/CSS/画像）自体は通常通りキャッシュされるので、そちらの速度は犠牲にしていません。

**ページネーション**：予定のログや運用ログのように件数が増え続けるデータは、「まず新しい分（未来の予定など）を全部返し、過去分は必要になった分だけ少しずつ追加読み込みする」設計にして、初回表示を軽くしています。

**アイコンの内製化**：以前は絵文字を使っていた箇所を、固定のSVG文字列を返す自作の`Icons.js`に置き換えました。画像ファイルを何十個も別リクエストで読み込む必要が無くなり、フォントによって絵文字の見た目が環境ごとに変わってしまう問題も解消しています。

**操作直後にすぐフィードバックを出す**：メニューをタップした瞬間に、実際のページ遷移を待たずローディング表示を即座に出す、といった細かい体感速度の工夫も各所に入れています。

## 5. エラー・想定外の動作への対応

**JSが壊れていても最悪詰まないようにする**：各ページの一番最初に「読み込み中…」のオーバーレイを仕込んでいて、通常はJSの初期化が成功した瞬間に消えます。ただ、もしJSファイルの読み込み失敗や構文エラーが起きると、このオーバーレイが画面全体を覆ったまま二度と消えず、ボタンが一切押せなくなる、という事故が過去に何度かありました（一括置換スクリプトのミスなど、人為的ミスが主な原因）。今は外部JSに一切依存しないインラインscriptで、数秒後に「そのまま操作する」ボタンを出し、さらに時間が経てば自動でオーバーレイ自体を消す、という二段構えの保険を入れています。

**JSが完全に死んでいても問題報告できるようにする**：上記のオーバーレイの中にある「問題を報告する」フォームは、`fetch()`を一切使わないただの`<form method="POST">`です。JavaScriptが1行も動かなくても、ブラウザ標準の機能だけで管理者にDiscord DMが飛びます。

**運用ログ（監査ログ）**：予定・時間割・カードデッキ・お知らせなどの変更は、GitHubのコミット画面のように「何が追加/削除されたか」を1件ずつ記録しています（サービス情報ページの「運用ログ」タブから誰でも見られます）。これは「何かおかしくなったときに、いつ・何が・どう変わったかを追える」ようにするための仕組みで、セキュリティというより運用上の説明責任（accountability）に近い話です。

---

技術的な質問・ツッコミ・「ここはこうした方がいいのでは」みたいな指摘も歓迎です。気になったところがあれば聞いてください。
