前回書いた「[1I勉強会webの裏側（設計の話）]」は、データの持ち方・Discord連携・セキュリティ・高速化の工夫といった、どちらかというと**裏側（サーバー・データ）寄り**の話でした。今回はその続編として、**サイトそのもの（フロントエンド）** の話を書きます。「どんなページがあるか」「どう作られていて、どこで動いているか」を中心に、こちらも気になるところだけ拾い読みしてもらえれば大丈夫です。

## 1. ページの一覧と、それぞれ何ができるか

このサイトは1枚のアプリ（SPA）ではなく、**機能ごとに素朴なHTMLファイルが分かれた昔ながらの構成**です。ページ遷移は普通のリンク・`location.href`で、フレームワークのルーターは使っていません。

| ファイル | ページ | 内容 |
|---|---|---|
| `index.html` | 予定管理 | 宿題・提出・テストなどの予定の一覧・追加・編集・削除 |
| `Timetable.html` | 時間割 | 通常の時間割＋休講・時限変更などの上書き、学期ごとの基本時間割の編集 |
| `Cardmaker.html` | CardMaker | 暗記カード（フラッシュカード）の作成・共同編集・フォルダ整理・CSVインポート |
| `Notice.html` | お知らせ | Markdown対応のお知らせ投稿・閲覧・実行済みチェック |
| `StudyLog.html` | 勉強ログ | 勉強タイマー・記録・ポイント・「みんなの記録」 |
| `Quiz.html` | みんなでクイズ | CardMakerのデッキを使ったクイズ対戦（独立した見た目・独自モーダル） |
| `ServiceInfo.html` | サービス情報 | Bot/Webのアップデート履歴＋「運用ログ」タブ（前回の記事の話） |
| `Login.html` | ログイン | Discord OAuth2でのログイン導線 |
| `DeleteApproval.html` | 削除の確認 | 本人以外がデッキ／お知らせを削除しようとしたときの、DiscordのDMリンク先 |
| `ShareApproval.html` | 共有の確認 | 本人以外がCardMakerのデッキ共有リンクを発行したがっているときの、DiscordのDMリンク先 |
| `DeckShare.html` | 共有デッキ閲覧 | CardMakerで発行された共有リンクの閲覧先。ログイン不要・そのデッキ1件だけを閲覧専用で見せる |
| `TanCheck.html` | 単位チェッカー | 情報工学科1年生向け、評価割合から達成度を計算するツール（後述、少し毛色が違う） |

ドロワーメニュー（左から出てくるナビ）を持つのは上記のうちログインが絡む6ページ（index/Timetable/Cardmaker/Notice/StudyLog/ServiceInfo）で、Quiz.html・Login.html・DeleteApproval.html・ShareApproval.html・DeckShare.html・TanCheck.htmlは単発の目的ページなのでドロワー自体を持ちません（`DeckShare.html`はそもそも予定など他の情報を一切見せない設計のため、ドロワーそのものが存在しません）。

### CardMakerだけ規模が違う

`Cardmaker.js`は単体で5000行を超えていて、他の主要ページ（`Plan.js`が1200行、`Timetable.js`と`StudyLog.js`が1800行台）と比べても頭一つ抜けています。これは機能を無理に1ファイルに詰め込んでいるからではなく、周辺機能を最初から別ファイルに切り出しているからです。

- `Cardmaker-csvimport.js` … CSVからのデッキ一括作成
- `Cardmaker-cardreorder.js` … カードの並べ替え（ドラッグ操作）
- `Cardmaker-search.js` … デッキ内検索
- `Cardmaker-listview.js` … 一覧表示の見た目切り替え
- `Cardmaker-quizplay.js` … CardMaker内でのクイズ再生ロジック（`Quiz.js`とは別物）

`Cardmaker.css`も専用に切ってあり、他ページ共通の`Style.css`とは独立したデザインで動いています（後述のDialog.js/Dropdown.jsを使わず、独自の`showCmConfirm`等を持っているのもこのため）。

## 2. どう作られているか（技術構成）

**フレームワーク・ビルドツールは一切使っていません。** React/Vueはもちろん、bundler（Webpack/Vite等）もTypeScriptもなく、素のHTML/CSS/JavaScriptをそのままブラウザに読ませています。`npm install`のようなビルド手順が存在しないので、ファイルを編集してpushすればそれがそのまま本番の中身になります（11人規模の運用に対して、ビルドパイプラインの保守コストは見合わないという判断です）。

### ページ専用JS + 全ページ共通JSの2層構造

各ページは「そのページ専用のJS（`Plan.js`・`Timetable.js`等）」に加えて、複数ページで共通して読み込む小さなスクリプト群を持っています。

- `Icons.js` … 絵文字の代わりに使う自作のSVGアイコン集
- `Dialog.js` … OS標準の`confirm()`/`alert()`/`prompt()`をサイトのデザインで置き換えたもの
- `Dropdown.js` … OS標準の`<select>`をサイトのデザインの見た目に差し替えるもの
- `SwipeGuard.js` … スマホの「画面端スワイプで戻る」ジェスチャーを抑制するもの
- `PendingDeleteCheck.js` … 自分宛の未確認の削除依頼が無いか、ページを開くたびにチェックするもの

これらは`index.html`だと`Icons.js → SwipeGuard.js → Dialog.js → Dropdown.js → Plan.js（ページ専用） → PendingDeleteCheck.js`の順で読み込まれています。共通スクリプトは全ページの`<script>`の型（`API_BASE`・`GUILD_ID`・`SESSION_KEY`のようなグローバル定数）を`const`で再宣言すると衝突してページ全体が壊れるため、値をリテラルのまま使うか専用のプレフィックスを付けた関数名にする、というルールで統一されています。

### OS標準UIから脱却している理由

`Dialog.js`と`Dropdown.js`は、「見た目をできるだけOSやブラウザの標準UIに頼らず、サイトのデザインで統一したい」という方針で作られたものです。特に`Dropdown.js`は元の`<select>`要素をDOM上には残したまま見た目だけ差し替える**段階的強化（progressive enhancement）**方式になっていて、スクリプトが読み込めなくても普通の`<select>`として動き続けるフェイルセーフ設計です。CardMakerとQuizだけは元々デザインが独立しているため、これらの共通部品は使わず、それぞれ独自のモーダル実装（`showCmConfirm`、Quiz.jsの`#qz-confirm-overlay`）を持っています。

## 3. どこで動いているか（ホスティングとデプロイ）

サイト本体は**Cloudflare Pages**（`wrangler.toml`の`name = "1studyweb32600"`）で、静的ファイルをそのまま配信しています。ビルドが無いので、Cloudflare側のGit連携で`main`へのpushをトリガーに、リポジトリの中身をほぼそのまま公開する形です。

### バックエンドへの中継役：`functions/api/[[path]].js`

Discord Bot本体（`bot.py`）はサーバー上でTailscale Funnel経由のホスト名で動いていますが、フロントのJSがそのホスト名を直接`fetch()`していると、ブラウザの通信先としてそのまま見えてしまいます。これを避けるため、Cloudflare Pages Functionsの機能で`/api/*`宛のリクエストを同一オリジンで受け止め、裏でバックエンドへ中継する**リバースプロキシ**を1枚挟んでいます。

- フロント側は常に`/api/get_study_data`のような相対パスだけを扱えばよく、実ホスト名はこのファイル1箇所にしか出てきません。
- `/api/events`（お知らせ等のライブ更新に使うSSE）もそのまま中継されます。レスポンスのbodyを一度読み込んで再構築するとストリーミングが壊れる（バッファリングされてしまう）ため、バックエンドの`Response`をそのまま包んで返す実装になっています。
- バックエンド側はログイン試行などのレート制限にIPアドレスを使っていますが、何もしないとこのプロキシ経由のリクエストが全部同じ発信元IPに見えてしまいます。そのためCloudflareが教えてくれる実クライアントIP（`CF-Connecting-IP`）を`X-Forwarded-For`として明示的に付け替えています。

### `_headers`ファイル

Cloudflare Pages特有の`_headers`ファイルで、全ページ共通のセキュリティ関連レスポンスヘッダー（CSP・`X-Frame-Options`・HSTS等）を設定しています。中身は前回の記事のセキュリティの章と同じものですが、`script-src`/`style-src`/`font-src`に`cdn.jsdelivr.net`と`cdnjs.cloudflare.com`が許可リストとして入っているのは、お知らせのMarkdown本文をサニタイズするのに使っているDOMPurifyなど、外部CDNから読み込んでいるライブラリのためです。

## 4. スマホアプリっぽい体験を作る細工

サイトはPWA（マニフェスト・Service Worker）ではありませんが、「ホーム画面に追加して、なるべくネイティブアプリっぽく使う」ことを意識した作り込みがいくつか入っています。

- **ホーム画面アイコン**：`apple-touch-icon.png`を用意し、iPhoneでホーム画面に追加したときのアイコンを指定。
- **画面端スワイプでの誤った「戻る」を防ぐ**：`SwipeGuard.js`が、画面端付近から始まる横方向優位のタッチ移動だけを`touchmove`で`preventDefault()`し、iOS/Androidの「エッジスワイプで前のページに戻る」ジェスチャーの引き金を止めます。PCのトラックパッド2本指スワイプ向けには、CSSの`overscroll-behavior-x: none`で同じことを別手段で実現しています（完全に防げる保証はなく、OS・ブラウザのバージョンによっては効かないこともあります）。
- **iPhoneのアドレスバー・ホームインジケーター対策**：ドロワー下部が画面外に隠れる不具合があったため、`bottom:0`頼みの高さ指定から`height: 100dvh`（今実際に見えている高さ）優先に変更。あわせて`viewport-fit=cover`と`env(safe-area-inset-bottom)`で、ホームインジケーターとの重なりも防いでいます。
- **ドロワー下部のログイン状態表示**：色付きの丸アバター（ニックネーム先頭2文字）＋ニックネーム＋学籍番号を表示し、タップで「アカウント設定」「ログアウト」を開けるミニメニューになっています。アカウント設定自体の実装は`StudyLog.js`にしか無く、他ページからは`/StudyLog.html?openAccount=1`に遷移してモーダルを自動オープンさせる方式で、6ページ分の複製を避けています。

## 5. 認証まわりのフロント実装

ログインの仕組み自体（トークンの形式など）は前回の記事に書いた通りですが、フロント側は次のようなパターンで統一しています。

- 変更系の操作（予定の追加、カードの保存など）を送る関数の先頭で`requireLoginOrRedirect()`を呼び、未ログインならログイン画面へ誘導してから処理を中断する。閲覧用の関数はこの対象外で、ログインなしでも予定一覧などは見られる。
- ログイン状態の判定は、StudyLog.js/Cardmaker.jsは元々持っている`STUDENT`オブジェクトを、それ以外のページは共通の`getLoginSession()`を使う（実装が2系統ある状態で、統一はまだされていません）。
- `Login.html`自体はDiscordの認可画面に飛ばすだけの導線で、認可が終わって戻ってきたあとの処理（トークン発行・保存）を担当しています。

## 6. サーバーを使わない機能もある：TanCheck（単位チェッカー）

`TanCheck.html`は他のページと毛色が違っていて、**バックエンドに保存しない**設計です。入力した点数はこの端末の`localStorage`にだけ残り、他の人とは共有されません。ログイン自体は他ページと同じ仕組みを流用していますが、それは「情報工学科1年生だけに公開する」ための出入り口として使っているだけで、サーバー側にこの機能専用のAPIは存在しません。

評価割合のデータ（定期試験○%・課題○%…といった内訳）は、高専機構の公開シラバス検索システムから前期・後期それぞれ実際に確認して手作業で転記したもので、シラバスの変更や担当教員による運用の違いは反映されません。あくまで参考値として使う前提のツールです。

---

前回の記事（データ・Discord連携・セキュリティ）と合わせて読むと、裏側から画面側まで一通りカバーできると思います。こちらも技術的な質問・指摘があれば気軽にどうぞ。
